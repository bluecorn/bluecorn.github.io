name: bluecorn_github_io
description: The bluecorn.github.io site, built with Jaspr in static mode.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  jaspr: ^0.23.1

dev_dependencies:
  build_runner: ^2.10.0
  build_web_compilers: ^4.4.6
  jaspr_builder: ^0.23.1
  jaspr_cli: ^0.23.1
  lints: ^6.1.0

jaspr:
  mode: static
